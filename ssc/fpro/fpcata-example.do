
*** Load example dataset
*use fpdata-1, clear
*use fpdata-2, clear

////////////////////////////////////////////////////////////////////////////////
* One threshold / No size / No weights / No quintiles 
fpcata, totexp(hh_expd) hexp(hh_hexpd) thresh(0.15 0.25)	

////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
* Multiple threshold / HH size / sampling weights / No quintiles 
fpcata, totexp(hh_expd) hexp(hh_hexpd) hhsize(hh_size) hhweight(hh_sampleweight) thresh(0.15 0.25 0.5)

////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
* Multiple threshold / HH size / HH weights / quintiles 
fpcata, totexp(hh_expd) hexp(hh_hexpd) thresh(0.15 0.25 0.5) hhsize(hh_size) hhweight(hh_sampleweight) q	

////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
* Multiple threshold / HH size / HH weights / quintiles --EXPORT--
fpcata, totexp(hh_expd) hexp(hh_hexpd) thresh(0.15 0.25 0.5) hhsize(hh_size) hhweight(hh_sampleweight) q e	

////////////////////////////////////////////////////////////////////////////////
